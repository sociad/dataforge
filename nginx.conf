server {
    listen 80;
    server_name 192.168.1.24;

    root /home/pi/projects/dataforge/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}
